<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container-fluid">
    <%= link_to "St", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <% if user_signed_in? %>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item <%= "active" if request.path == my_stocks_path %>">
            <%= link_to "My stocks", my_stocks_path, class: "nav-link" %>
          </li>
          <li class="nav-item <%= "active" if request.path == my_friends_path %>">
            <%= link_to "My friends", my_friends_path, class: "nav-link" %>
          </li>
          <li class="nav-item <%= "active" if request.path == search_friends_path %>">
            <%= link_to "Search friends", search_friends_path, class: "nav-link" %>
          </li>
        </ul>

        <div class="navbar-nav">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-expanded="false">
                <%= fa_icon "user" %> <%= current_user.full_name %>
              </a>
              <div class="dropdown-menu">
                <%= link_to edit_user_registration_path, class: "dropdown-item" do %>
                  <%= fa_icon 'pencil' %> Edit profile
                <% end %>

                <%= link_to destroy_user_session_path, method: :delete, class: "dropdown-item" do %>
                  <%= fa_icon "user-o" %> Sign out
                <% end %>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item disabled" href="#">View profile</a>
              </div>
            </li>
            <li class="nav-item">
              <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
            </li> 
          </ul>
        </div>
        
      </div>
    <% end %>
  </div>
</nav>
<hr />
